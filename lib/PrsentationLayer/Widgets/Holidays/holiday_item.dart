import 'package:flutter/material.dart';

class HolidayItem extends StatelessWidget {
  const HolidayItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
